import React from 'react';

// type Props = {}

export default function Header() {
  const headerStyle = {
    backgroundColor: 'greenyellow',
    fontWeight: 800,
  };

  return <header style={headerStyle}>Products list</header>;
}
